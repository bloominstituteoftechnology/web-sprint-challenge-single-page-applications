describe("Quotes app", () => {
    beforeEach(() => {
        
        cy.visit("http://localhost:1234");
      });
    
    const nameInput = ()=>cy.get('imput[name="name"');
    const sizeInput = ()=>cy.get('imput[name="size"');
    const Input = ()=>cy.get('imput[name="name"');
    const 


});