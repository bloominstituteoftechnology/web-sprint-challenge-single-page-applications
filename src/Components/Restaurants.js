import React from 'react';
import styled from 'styled-components';


export default function Restaurants (props) {
    //Destructure the incoming props to use them later
    const {restaurants} = props;
    
    return (
        <>

        </>

    )
}