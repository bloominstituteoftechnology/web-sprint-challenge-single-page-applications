import React from "react";
import { NavLink } from "react-router-dom";

export default function HomePage() {
    return(
        <header>
            <h1>Lambda Eats</h1>
            <a>Home</a>
        </header>
    )
}