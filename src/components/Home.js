import React from 'react';
import '../components/home.css';
import { Route, Link} from 'react-router-dom';
import './home.css';
const HomePage = (props) => {
    
    return(
        <div >
            <h1> Make Your own Pizza Special</h1>
        </div>

    );
}

export default HomePage;